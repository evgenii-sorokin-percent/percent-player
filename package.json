{
  "name": "percent-player",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -F server dev & pnpm -F client dev",
    "dev:server": "pnpm -F server dev",
    "dev:client": "pnpm -F client dev",
    "build": "pnpm -F server build & pnpm -F client build & pnpm build:sea-config & pnpm build:sea",
    "build:server": "pnpm -F server build",
    "build:client": "pnpm -F client build",
    "build:sea-config": "node build-sea-config.ts",
    "build:sea": "node --build-sea sea-config.json",
    "lint": "pnpm -F server lint & pnpm -F client lint",
    "lint:server": "pnpm -F server lint",
    "lint:client": "pnpm -F client lint",
    "lint:fix": "pnpm -F server lint:fix & pnpm -F client lint:fix",
    "lint:fix:server": "pnpm -F server lint:fix",
    "lint:fix:client": "pnpm -F client lint:fix",
    "format": "pnpm -F server format & pnpm -F client format",
    "format:server": "pnpm -F server format",
    "format:client": "pnpm -F client format",
    "format:check": "pnpm -F server format:check & pnpm -F client format:check",
    "format:check:server": "pnpm -F server format:check",
    "format:check:client": "pnpm -F client format:check",
    "prepare": "husky",
    "commitlint": "commitlint --edit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.24.0",
      "onFail": "download"
    }
  },
  "packageManager": "pnpm@11.24.0",
  "type": "module",
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "@types/node": "^26.4.0",
    "consola": "^3.4.2",
    "husky": "^9.1.7",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "typescript": "^7.0.2"
  }
}
